import React, { useEffect, useState } from 'react';
import { Link } from 'react-router-dom';
import { Button, Dimmer, Loader, Card } from 'semantic-ui-react';

export default ({ campaign, accounts }) => {
	// useEffect(() => {
	// 	const loadRequests = async () => {
	// 		if (campaign) {
	// 			const requests = await campaign.methods.getRequests().call();
	// 			if (requests.length) setRequests(requests);
	// 		}
	// 	};
	// 	loadRequests();
	// }, [factoryContract]);

	return <></>;
};
